import type {Metadata} from "next";

export let metadata: Metadata = {
  title: "Calculator",
  description: "Calculate your macros and calories",
};

export default function CalculatorPage() {
  return <div>CalculatorPAge</div>;
}
