// TODO Only Admin will be able to create users

import {PageWrapper} from "@/_components/page-wrapper";

export default function RegisterPage() {
  // TODO check if user is admin, else redirect to home page

  return <PageWrapper>RegisterPage</PageWrapper>;
}
