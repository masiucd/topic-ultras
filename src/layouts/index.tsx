import * as React from 'react'
import Helmet from 'react-helmet'

import 'modern-normalize'
import '../styles/normalize'

const IndexLayout: React.FC = ({ children }) => <h1>hello</h1>

export default IndexLayout
